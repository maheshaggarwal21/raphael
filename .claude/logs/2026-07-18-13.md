# Session 13 — 2026-07-18

Owner directive (verbatim intent): the architecture has a major flaw — almost all the
work is manual (approving lessons, running commands, atlas, etc.), which will lose
users. Wanted: one-time install, one up-front permission grant, then Raphael runs on
its own — INCLUDING auto-approving security lessons — and the user only notices the
result (fewer tokens, production-grade quality/security). Task: brainstorm + propose a
proper architecture and change plan.

## 1. Autopilot vision + change plan (COMPLETE — proposal delivered)

- docs/autopilot-vision.md NEW: the full design.
  - Honest inventory: 9 of 11 lifecycle steps are manual today; only injection and
    guard are automatic — exactly the two a user would keep.
  - Target spec: ask once, act always, show weekly, undo anytime.
  - Core design move: a MACHINE CURATOR replaces the human queue instead of deleting
    curation — existing gates + reviewer screen (reused from adopt, fail-closed) +
    dry-run canary gate with batch rollback + probation confidence + auto-retire
    sweep + git audit trail + one-click undo.
  - `raph pulse` = the only new verb: SessionEnd hook -> detached child -> mine ->
    budget/E-LIMIT check -> distill -> curate -> atlas refresh -> compile -> one
    pulse event. Every step fails open. No daemon, no scheduler, event-driven only.
  - Global consent (`consent: all` + ignore list) + `mode: autopilot` + dial FULL.
  - Onboarding happens IN the chat: first-SessionStart envelope has the agent ask
    the one consent question, then `raph arise --autopilot`. Never asks again.
  - Passive visibility: ≤150-token weekly digest (7-day throttle, silent when
    empty, security lessons always highlighted); console becomes the read-mostly
    window with one-click undo.
  - Security lessons AUTO-APPROVED per the owner's explicit call — recorded as a
    REVERSAL of §11.11 (to be written as ARCHITECTURE §11.13 when 17.1 builds);
    residual risk stated plainly in the doc (advisory-only + audit + undo bound it).
  - ONE floor kept: quarantined (injection-suspect) content never machine-activates
    — but it never nags either (silent, digest-counted, 30-day tombstone). Rare
    (2 items ever). Reasoning: no machine screen can clear content whose defining
    property is that it tried to manipulate the machine.
- TASKS.md: Phase 17 "Autopilot" added as PROPOSED, 6 milestones in dependency
  order (consent/mode -> machine curator BEFORE the unattended loop -> pulse+hook ->
  atlas-in-pulse -> onboarding+digest -> default flip + v0.2.0). ~6 sessions.
- Build NOT started — the ask was the brainstorm + plan; awaiting owner go.

## 2. Round 2 — owner's two-brain spec folded in (COMPLETE)

Owner clarified the full product model:
- TWO BRAINS: global (GitHub, owner-curated, upgraded over time) + local (each
  user's device, starts as a COPY of the global brain, then learns locally).
- THREE install permissions: (1) learn from my work — compulsory, includes
  down-sync updates; (2) contribute local lessons to the global brain in
  occasional bundles — optional, denied = lessons never leave the device;
  (3) mode autopilot/manual — autopilot default + recommended.
- TWO usage surfaces, same feeding: normal chat (hooks inject lessons/atlas/
  digest automatically) and Raphael's shipped agents (same + sharper: PreToolUse
  hands the atlas to an agent about to Grep — live since 16.3).
- Goal restated: fewer tokens, better code quality, production-grade security.

Folded into docs/autopilot-vision.md (§2.1 two-brain model w/ down-sync/up-sync
mechanics, §2.2 permissions, §2.3 surfaces; onboarding + change plan updated) and
TASKS Phase 17 (new 17.6 global brain repo+seed+down-sync, 17.7 contribution
bundles, flip renumbered 17.8; ~8 sessions total). Mechanics decisions (mine,
recorded for owner veto): down-sync via npm snapshot + pinned GitHub manifest
(invariant #5c amendment); up-sync v0 = staged bundle + one-click send, v1 =
owner-deployed serverless ingest endpoint opening submission PRs (zero user auth,
owner curates everything entering the global brain). Invariant #6 amendment:
contribution opt-in moves to the install grant, per-lesson exclusion stays.

## Next
- On owner go: build 17.1 -> 17.8 in order (17.2 strictly before 17.3 so the
  unattended loop never exists without its safety layer; 17.6's seed half before
  the 17.8 flip so new installs start from the global brain).

## 3. BUILD GO — Phase 17 started (owner: "build it end to end, stop only after
## development and testing, test from an outside user's perspective")

### 17.1 SHIPPED (364/364)
- config.js: getMode/setMode (fail-closed curator), hasConsent (explicit answer >
  ignore-subtree > scope:all > undefined), setConsentScope; mine.js uses hasConsent
  (global grant covers new projects; explicit "no" and ignore subtrees still win).
- autoapprove.js: DIAL 'full' (adopted rides; security never rides the PLAIN dial —
  at full the skip points to the curator path; quarantined never anywhere).
- auto.js: `raph auto full` = autopilot ON (mode+dial coupled, clear print);
  manual/off/standard/wide = curator. Console levels derive from DIAL_LEVELS (free).
- ARCHITECTURE §11.13: machine curator supersedes §11.11 in autopilot; quarantine
  floor survives in every mode. Fix: web.test levels expectation +'full'.

### 17.2 SHIPPED (374/374)
- lib/curator.js: curateStaged = the one autopilot activation entry. Delegates to the
  plain dial below autopilot+full (zero model calls, tested); at full: reviewer screen
  per candidate (REVIEW_TOOL schema reused from adopt, malformed/transport = fail
  closed, E-LIMIT propagates, security addendum = DEFENSIVE/GENERIC/advisory max
  strictness, quality>=1) -> tier 'machine' activation -> CANARY GATE (chokepoint
  canaries + index rebuild) -> failure = whole-batch rollback, candidates restored
  byte-identical, zero phantom events (events+commit only after the gate).
- sweepQuarantine: 30-day silent tombstone into rejection memory (mtime-based).
- Schema tier enum +'machine'; E-AUTOSEC unchanged (tier auto only) — verified both
  directions. confidence: machine tier gets the 0.9 probation discount. countAutoTier
  counts auto+machine (shared cap). distill/adopt commands -> await curateStaged.
- Test gotcha: "ignore all previous instructions" is E-DENY (hard reject), not
  quarantine — quarantine fixtures need W-IMPERATIVE "you must" phrasing instead.

### 17.3 SHIPPED (382/382, live-smoked)
- lib/pulse.js: runPulse = the heartbeat. Gates: autopilot mode + hasConsent (never
  grants, silent skips) + lock (wx create, 30-min stale steal). Steps, each fail-open:
  mine (ledger-incremental) -> budget (8 episodes/pulse, 3 distill runs/day from pulse
  events) -> distill --yes (curator inside; exit 4 = limited, ledgers resume) ->
  sweepQuarantine -> probationRetire (machine/auto tiers only, max 3) -> buildIndex ->
  one pulse event. commands/pulse.js: --async (hook entry: stdin JSON cwd, detached
  spawn -> logs/pulse.log, exit 0 always), --run (inline), status view. SessionEnd
  hook added to plugin/hooks.json. cli.js: pulse registered + help.
- LIVE: --async returned instantly, detached child executed + logged; curator-mode and
  no-consent runs skip silently. BONUS: smoke caught a PRE-EXISTING auto.js bug — the
  level word was never parsed when --cap absent (capIdx+1===0 excluded args[0]); fixed
  with index-based exclusion. `raph auto full` now verified live end to end.

### 17.4 SHIPPED (384/384, live-verified)
- Atlas persistence moved to lib/atlas.js (atlasPaths/loadAtlasDoc/buildAndSaveAtlas);
  commands/atlas.js is a thin skin again (kept mapFileName/atomicWrite for the vault
  export path — two import regressions caught while thinning). gitHead() stamped in
  every atlas doc; refreshAtlasIfStale(): missing / HEAD-moved / daily-for-non-git.
- Pulse step 5 = atlas freshness (DI for tests, summary.atlas in the event). Live:
  raphael atlas rebuilt incrementally (186 files, 116 cache-reused, 0 tokens), second
  run = "fresh" no-op. Sessions now start with a current atlas digest with no human
  ever running `raph atlas`.

### 17.5 SHIPPED (392/392, live-smoked)
- onboardingBlock(): one-time envelope on the first session-start of a fresh install
  (no config.yaml; marker makes it once-ever even if ignored — zero nag). Instructs
  the agent to ask the three permissions in plain words and run arise accordingly.
  Fires BEFORE the empty-index gate (fresh brains have no lessons).
- weeklyDigestBlock(): autopilot-only, 7-day throttle (digest-shown events), silent
  when the week was empty, <=150 tokens, security always called out; honest numbers
  from the events ledger. Appended to session-start after atlas + decisions.
- arise --autopilot [--contribute] [--guard]: records permission 1 (consent scope
  all), 2 (contribute.enabled), 3 (mode autopilot + dial full) and prints the
  zero-touch contract. Manual arise unchanged. Live smoke verified the full print.
- Existing inject tests: sandboxes now write config.yaml (missing config = fresh
  install = onboarding, which is the DESIGNED behavior). Naming collision (digest)
  caught by node's parser -> renamed local to `weekly`.

### 17.6 SHIPPED (398/398, live-verified)
- global-brain/ committed: lessons.json (26 lessons, FIXED ids preserved across
  regens) + manifest.json v1 (per-lesson sha256 over canonical JSON — EOL-proof by
  design; git CRLF conversion can't break verification). scripts/build-global-brain.mjs
  regenerates from PACK_SPECS; version bumps only on real content change (rerun = no-op,
  verified); every lesson passes the chokepoint AT BUILD TIME too.
- lib/globalbrain.js: activateGlobalLessons (hash verify -> local-wins dedupe by
  slug+id -> validateLesson always -> tier curated active), seedGlobalBrain (packaged
  copy, zero network; arise --autopilot calls it — LIVE: fresh install = 26 active
  curated lessons in one command, cold-start solved), syncGlobalBrain (weekly throttle,
  the TWO pinned URLs only, version short-circuit, malformed refused, offline fail-open
  w/ last_check advance so no hammering). Pulse step 5 (DI-able) + summary.globalSync.
- package.json ships global-brain/; CLAUDE.md invariant #5c recorded.

### 17.7 SHIPPED (402/402, live-smoked)
- Bundles in lib/contribute.js: permission #2 gate (default OFF = refuse, nothing
  staged); eligibility = active + tier!=curated (global lessons never bounce back) +
  not already bundled; each lesson through the EXISTING export pipeline (strip ->
  re-scrub -> re-validate; failures skipped, never raw — test embeds an AKIA key and
  proves no raw key can leave); staged locally as bundle-<ulid>.json + event.
- maybeBundleContributions in pulse (weekly, >=3): stage-only — pulse still performs
  ZERO network writes. `raph contribute bundle|send`; send lists bundles + the GitHub
  issue path (user's browser act; maintainer curates). Digest mentions staged bundles.
- Live: post-arise brain has 26 curated lessons and 0 eligible — exclusion verified.

### 17.8 SHIPPED — flip + docs + OUTSIDE-USER E2E + v0.2.0 (402/402)
- Docs: README = "install and forget" (autopilot lead, global brain, machine curator,
  honest security model); manual.md new §0 autopilot + §10 hooks; docs/hooks.md
  +SessionEnd fallback. Policy decision recorded: bare `raph arise` stays MANUAL on
  purpose — a CLI default must never grant consent silently; the in-chat onboarding
  (which asks first) is the autopilot funnel and marks it recommended.
- OUTSIDE-USER E2E — clean npm prefix, `npm pack` 0.2.0 tarball installed globally,
  sandbox RAPHAEL_HOME, exactly a stranger's path:
  1. raph version 0.2.0; doctor pre-setup fails = correct fresh-machine state
  2. first session-start -> <raphael-onboarding> (3 questions); second -> silent
  3. arise --autopilot --contribute -> CONSENT/SHARE/MODE/SEED 26 -> doctor HEALTHY
  4. next session-start -> real <raphael-lessons> recall (26 lessons, IDOR headline)
  5. raph auto -> AUTOPILOT · full
  6. SessionEnd `pulse --async` (real Windows cwd like Claude Code sends) -> detached
     child ran: mine (no transcripts, correct), atlas built, pulse event + status + log
  7. next session-start -> <raphael-digest> with honest numbers (341 recall tokens)
  8. contribute send -> clean (0 eligible: curated lessons never bounce back)
- E2E CAUGHT A REAL BUG: pulse --async passed the project as spawn `cwd`; a bad/
  nonexistent path (Git Bash /tmp mistranslation in the test, but any vanished dir in
  production) made the DETACHED CHILD DIE SILENTLY (spawn ENOENT, swallowed by the
  fail-open catch). Fix: no cwd option — the project travels via --project only.
  Repacked + reinstalled + re-verified end to end.
- package.json -> 0.2.0. Tarball 300.4 kB / 144 files incl. global-brain/.

### Phase 17 CLOSEOUT — CI green + v0.2.0 released
- CI run 1 on the 17.8 commit FAILED all 6 jobs: plugin.json/marketplace.json still
  0.1.0 vs the version-lock test. Root cause of the local miss: I piped npm test
  through `tail -3`, which cut off the `# fail` line — a red suite read as green.
  Fixed manifests -> 402/402 with `# fail 0` asserted explicitly -> CI ALL 6 GREEN.
- The miss itself became a lesson: "Truncated test output hides failures" written
  via `raph note` + approved -> BRAIN 59 ACTIVE. (Flywheel: the build's own slip
  feeds the brain that prevents it next time.)
- GitHub Release v0.2.0 "Autopilot" created on the green commit.
- REMAINING OWNER SWITCH: `npm publish` (raphael-brain@0.2.0) — credential action,
  never mine. Everything else is done, tested, and public.

### v0.2.0 LAUNCHED — owner ran `npm publish` (the one credential action)
- prepublishOnly showed 402/402 in the publish log; raphael-brain@0.2.0 shipped
  (144 files, 300.6 kB, tag latest, incl. global-brain/).
- Registry verified: npm view = 0.2.0 / latest.
- END-USER PATH LIVE from the public registry: npm install -g raphael-brain ->
  raph version 0.2.0 -> arise --autopilot seeds 26 curated lessons from the
  PUBLISHED package -> doctor healthy. The autopilot install path works for
  strangers today. PHASE 17 FULLY COMPLETE — nothing remains.

## Post-launch docs pass (owner ask: outside-user-first docs + agents guide)
- README: new "Your first session — what to expect" block (is-it-on checks) and a
  full "The agents — 10 specialists that share your brain" section: how to invoke
  by name in Claude Code, a table of when-to-use + WHAT TO GIVE EACH AGENT in
  context, flagship order, recipes, slash commands. Numbers fixed (402 tests,
  40 verbs).
- manual.md: §2 intro now mode-aware (autopilot vs manual gate); `raph auto` section
  gained full/manual + the truthful security story per dial level; §10 expanded into
  a real agents guide (spine, invocation, per-agent input table, recipes); §11 safety
  model rewritten to match ARCHITECTURE (mode-conditional security activation,
  quarantine floor, three-item network list incl. pinned down-sync, bundle opt-in).
- handbook: updated for v0.2.0 throughout — Part 1 gate bullet + highlight reel
  (autopilot + global brain first) + 402 tests/npm-live; Part 2 dial has full, 2.13
  now explains how users invoke agents + what to hand each, NEW 2.18 Autopilot and
  2.19 Two brains; Part 3 split into Journey A (autopilot default, ~3 minutes of
  human action) and Journey B (Priya, manual); Part 4 Q1/Q5/Q8 refreshed (down-sync,
  402, quarantine-floor answer); Part 5 posts all lead with "install once, runs
  itself" + arise --autopilot.
- Checked: remaining "security always waits" phrasings are all correctly scoped to
  curator mode / plain dial. 402/402 green.

## Docs follow-up: where each command runs
- Owner ask: installation instructions must say WHERE to type each command.
- README install section split into "Step 1 — in your terminal" / "Step 2 — inside
  Claude Code (chat input, slash commands)" + a standing rule line (raph = terminal,
  /… = Claude Code); manual gained a where-commands-run table up top, an Installing
  note in §1, and the §10 install block labeled; handbook journeys A+B label
  terminal vs Claude Code per step. 402/402 green.

## Install-instructions fix: /plugin is not universal
- Owner hit it live: the desktop app has NO /plugin dialog ("/plugin isn't available
  in this environment"). Verified against code.claude.com docs + `claude plugin --help`
  (v2.1.168): the terminal CLI `claude plugin marketplace add` + `claude plugin
  install` exists and is the docs' own non-interactive path; /plugin only exists in
  interactive TERMINAL sessions.
- LIVE-VERIFIED the fix on this machine: `claude plugin marketplace add
  maheshaggarwal21/raphael` -> `claude plugin install raphael-brain@raphael` ->
  `claude plugin list` shows raphael-brain 0.2.0 enabled (user scope). The owner's
  plugin install is now actually complete.
- Docs updated to lead with the terminal path everywhere (/plugin kept as the
  noted alternative for terminal sessions): README step 2, manual (§1, §10,
  where-to-run table row for `claude plugin …`), hooks.md, handbook journeys A+B.
- arise.js next-steps output also printed /plugin — fixed to `claude plugin …`
  (ships with the next npm patch release; registry 0.2.0 still shows the old text).

## Contribute-by-default + console Phase-17 catch-up (owner directive)
- Owner ran arise --autopilot on the real brain, saw SHARE NOT granted, and directed:
  contribution should be ON by default (changeable later); also flagged the console
  Settings tab as outdated vs the new autopilot substrate.
- (a) arise --autopilot now grants permission #2 by default; --no-contribute opts out.
  Decision recorded as ARCHITECTURE §11.14 + CLAUDE.md invariant #6 update. Safety
  unchanged: grant = bundles STAGE locally only; sending is always the user's click.
- (b) New `raph contribute on|off` — lib/contribute.js setContribution() is the ONE
  grant writer (arise, the CLI verb, and the console all call it). Bundle error text
  + onboarding envelope (Q2 recommended/default-on; --no-contribute mapping) updated.
- (c) applyDial() extracted to lib/autoapprove.js: the dial+mode coupling
  (full=autopilot, sub-full=curator, manual steps full->standard) had lived only in
  commands/auto.js — the console's /api/auto was missing it (a real one-engine §14
  violation). Both faces now call applyDial.
- (d) Console Settings tab rebuilt for Phase 17: Mode card w/ autopilot<->manual
  switch, full-level radio properly described, security-floor paragraph corrected
  (E-AUTOSEC at off/standard/wide; machine curator at full; quarantine never),
  Community-sharing card (grant toggle via POST /api/contribute, staged bundle +
  eligible-lesson counts, stage-only language). settingsView() exposes
  mode+contribution.
- Tests 402 -> 405 (applyDial coupling, setContribution + arise default, console
  mode/contribute e2e); onboarding test flipped to the new default (it encoded the
  superseded policy). LIVE: `raph contribute on` on the real brain (granted
  2026-07-18, matches the owner's intent from their arise run); `raph web` smoked in
  the browser — Settings shows AUTOPILOT + GRANTED + 33 eligible, zero console
  errors. Server stopped after smoke.
- NOTE: the owner's pasted arise output showed /plugin lines because their global
  raph is registry 0.2.0 — both this and the /plugin fix ship at the next publish.

## v0.2.1 release prep
- Owner ran npm publish; refused ("cannot publish over 0.2.0") because the version
  was never bumped after today's fixes. Bumped 0.2.0 -> 0.2.1 in package.json +
  plugin/.claude-plugin/plugin.json + BOTH version fields in
  .claude-plugin/marketplace.json (the version-lock lesson from the v0.2.0 CI miss).
  0.2.1 carries: claude-plugin CLI install path (docs + arise output),
  contribute-on-by-default at arise --autopilot + raph contribute on|off,
  applyDial one-engine fix, console Settings catch-up, agents guide in README/manual.
  405/405. Owner re-runs npm publish for 0.2.1.
- Tag v0.2.1 pushed + GitHub Release created (target_commitish rejects short SHAs —
  tag first, then release without the field). Owner's npm publish is the last step.

## Guard auto-install (owner ask: guard should start automatically in any git repo)
- ensureGuard() added to lib/guard.js (idempotent: not-a-repo / present / foreign-hook
  [NEVER clobbered] / installed) + wired as pulse step 6c (autopilot+consent-gated like
  every pulse step, fail-open, opt-out autopilot.auto_guard:false, summary.guard +
  event field). So on autopilot, every consented git repo you work in gets the
  pre-commit secret hook with zero clicks the first time a session ends there.
- +1 test (406/406): install -> present -> foreign untouched -> non-repo no-op ->
  opt-out. LIVE smoke: real `raph pulse --run` on a scratch repo installed the hook
  ([guard] line + raphael-guard script on disk), no tokens spent.
- Docs: README loop diagram + manual §0/§7 + handbook 2.11/2.18. Version bumped
  0.2.1 -> 0.2.2 (0.2.1 is already on the registry); tag+release at publish time.

## Self-update / auto-upgrade (owner ask: never run npm update manually)
- src/lib/update.js: currentVersion (reads package.json), compareVersions (numeric
  semver, pre-release ignored, never downgrades), checkForUpdate (bounded GET of the
  npm registry doc for raphael-brain/latest — the ONLY endpoint — via fetchUrl),
  runNpmUpgrade (spawns `npm install -g raphael-brain@latest`; npm's sha512 integrity
  is the supply-chain gate; Windows npm.cmd+shell handled), maybeSelfUpdate (daily
  throttle via state/update.json, upgrade only when strictly behind, event logged,
  fail-open on offline/registry/npm errors).
- Wired as pulse step 8 (LAST on purpose: a mid-upgrade file swap can't disturb earlier
  steps). Autopilot+consent gated like every pulse step; opt-out autopilot.auto_update:false.
- `raph update [--check]` command (CLI now 41 verbs). Weekly digest surfaces
  "updated to vX". Invariant #5 -> #5d amended in CLAUDE.md + ARCHITECTURE §0.6.
- Tests: test/update.test.js (5) + pulse deps stub selfUpdate (no test hits the network)
  + digest self-update test. 406 -> 412, all green. LIVE: real `raph update --check`
  correctly reports up-to-date (dev 0.2.3 > registry 0.2.2, NO downgrade); maybeSelfUpdate
  end-to-end via fake registry + stub upgrade wrote state + logged 1 self-update event.
- Registry moved to 0.2.2 mid-session (owner published the guard release), so this rides
  a fresh bump 0.2.2 -> 0.2.3. Tag + GitHub release below.
- Tags + GitHub releases created: v0.2.2 (guard-installs-itself, at 96f523b) and
  v0.2.3 (auto-upgrade, at 309ff23). v0.2.2 had been published to npm without a tag;
  backfilled. Owner's `npm publish` for 0.2.3 is the last step.

## v0.2.3 PUBLISHED (owner ran npm publish)
- Registry = 0.2.3, dist-tags.latest = 0.2.3. End-user path verified from the PUBLIC
  registry in a clean prefix: npm install -g raphael-brain@latest -> raph version 0.2.3
  -> raph update --check = "up to date (0.2.3)". Auto-upgrade is live and self-consistent:
  existing autopilot users' next daily pulse pulls 0.2.3 on its own. Windows global prefix
  install lands at <prefix>/node_modules/raphael-brain/bin/raph.js (not lib/node_modules).
