# Session 2026-07-16-06

## Context
Owner delivered an expanded vision in their own words: Raphael as a self-sufficient
"company" — agent employees per specialty (memory/context/token/DB management, research,
marketing, finance, reports), an agent that makes agents, an agent that manages agents,
self-upgrading ("this library/stack is better"), full need-to-shipped-product handoff,
reusable self-made skills, self-record-keeping (agents/commands/projects/tokens) — PLUS a
knowledge inlet: drop a GitHub link/PDF/text/skill file found on social media and Raphael
adopts what's useful; and simple installation for others ("like gstack"). Asked for an
honest, realistic, organized brainstorm — no parallel agents (honored; all inline).

## Delivered
- docs/company-vision.md — the organized version. Highlights:
  - ~70% of the machinery already exists (roster, 3 shipped products, brain=37, stats,
    checkpoints, plugin); missing 30% = adopt pipeline, skills factory, coded autopilot,
    budgets/reports, meta-agents.
  - Org chart made real role-by-role with honest limits: marketing = content studio not
    media buyer; finance = token budgets not payments; DB agent for products, Raphael
    itself stays file-based.
  - Scout/adopt pipeline design: `raph adopt` -> lessons + skill drafts + worth-installing
    verdicts + (rare, license-checked, owner-approved) vendored code; provenance ledger;
    v1 LOCAL-ONLY so invariant #5 (model-only network) is untouched; fetched content is
    data (injection defense); no social-media auto-scrolling (accounts/ToS/junk).
  - Self-upgrade rule: no measurement, no mutation — eval harness is the QA department.
  - Missing pieces added: provenance ledger, budgets+scheduler (limits are the real
    constraint), weekly report, pruning, owner-as-CEO named explicitly, portfolio registry.
  - Build order: A=adopt v1 (recommended first), B=skills factory, C=company ops,
    D=Phase 11 publish.
- TASKS.md: Phase 13 (Scout) + Phase 14 (Company ops) added as PROPOSED.
- Memory raphael-vision.md updated with the 2026-07-16 expanded vision.

## Decisions surfaced to the owner (boundary items, not built)
- Invariant #5 amendment for network fetch in `raph adopt` v2 — owner sign-off required.
- npm publish timing (Track D). Everything else proceeds under the standing mandate.

## Round 2 — owner clarified + expanded (same session)
Owner decisions recorded: (1) CLI fetch ALLOWED — invariant #5 to be amended (user-
initiated, read-only, bounded); (2) "copying code" = read/understand external code then
patch RAPHAEL itself (idea adoption, self-improvement); (3) a WEBSITE for managing
Raphael — owner as global admin, users manage their own approvals/permissions; CLI stays;
(4) auto-approve must be a selectable option for owner AND users; (5) adopted info gets a
reviewer-agent screen before human approval.

Studied ARCHITECTURE.md §0/§9/§11 first (per project rules). Key finds: §9 auto mode +
restricted tier IS the auto-approve design already; §11.7 two-audience model maps onto
the two website roles; §11.8 privacy hard rule FORBIDS a central server holding user
data — resolved the website as LOCAL console per install (`raph web`, each user admins
their own brain) + THIN hosted hub for genuinely global parts (docs, pack registry,
contribution face; static-first). Owner's "global admin" = his own console + hub curator.

Delivered docs/web-console-vision.md: one-engine-three-faces principle (no verb, no
button); console page-by-page (dashboard, review queue, adopt inbox = killer page,
lessons browser, activity feed, portfolio, gallery, settings, guard); localhost security
(127.0.0.1 + token + Origin checks vs CSRF/DNS-rebinding; XSS hard line); auto-approve
as a DIAL (OFF/STANDARD/WIDE) with poisoning defenses (machine-approved tags, bulk
revoke-by-source, daily cap, quarantine delay); adopt v2 six-layer gauntlet (fetch ->
pre-gates -> reviewer agent -> distill -> chokepoint -> human/auto); read-understand-
patch pipeline (branch + tests + eval + human merge, chokepoint files heavyweight,
copyleft ports blocked); 10 edge cases incl. phone->PC gap, review flood, undo,
telemetry-vs-privacy, no-model degradation. Pushback recorded: security lessons +
self-patches should STAY human (one click on the console) — full auto there is an
invariant amendment only the owner can make explicitly.

TASKS.md: Phase 13 updated (fetch in v1, reviewer agent, patch proposals), Phase 15
(web console) added, owner-decisions-pending section added.

## What's next
Owner reads the brainstorm; then fold company-vision.md + web-console-vision.md into
ARCHITECTURE.md (new §13/§14 + invariant amendments) and a real TASKS phase plan; then
build Track A (adopt v2) -> Track B (console MVP). Two owner decisions pending: security
floor, hub scope.
