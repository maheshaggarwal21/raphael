# Session 14 — 2026-07-19

## Doc overhaul verification (owner did the writing, I verified tracking)

Owner rewrote README.md, ARCHITECTURE.md front matter, and added docs/README.md
directly (3 commits: `d57c6c6` major README rewrite — story-driven tour of all 16
subsystems incl. Raph Web console + Skill Factory + Agent Maker, by-the-numbers
proof section, full 41-verb command atlas, agents roster, FAQ; `5804570`
ARCHITECTURE.md overview/nav upgrade, byte-identical below the intro; `44e65f7`
README §4 reworked into the full two-brain story), pushed directly to origin/main.
My local clone was 3 commits behind — pulled (fast-forward, clean).

Verified:
- `npm test` → 415/415 green (`# fail 0` explicitly checked, not truncated — the
  v0.2.0 CI lesson).
- `raph doctor` → healthy (brain, config, git, guard, 60/60 lessons validate,
  plugin manifest/hooks all present).
- Version consistency: package.json / plugin.json / marketplace.json all 0.2.4 —
  no version-lock drift.
- Numeric claims in the new docs cross-checked against reality: "415 tests, 6 CI
  combos", "41 verbs" — both correct as of this commit.
- The two features the owner specifically flagged as previously missing are now
  well covered: `raph web` (3 mentions incl. a full "mission control" section),
  Skill Factory (7 mentions).
- Working tree clean, nothing uncommitted, local == origin/main at `44e65f7`.

No code changes this session — pure doc-tracking verification per the working
ritual. Task complete, clean + pushed (nothing further to push — owner already
did) — safe to /compact.

## Round 2 — v2 research + brainstorm (owner ask: make Raphael the industry standard)

Owner wants Raphael to become a developer "ritual," gave two Gemini deep-research
share links plus a pasted list of 10 fast-growing GitHub repos, and asked for a
grounded v2 expansion plan — psychology of why developers adopt these tools, ideas
for new/expanded Raphael functions, one hard constraint (token usage always lower
than not using Raphael), "luxury" as the attraction lever.

Gemini links (`share.gemini.google/ZbcmFAU5Q9Yv`, `.../HDfk9e3yhTbO`) redirect to
`gemini.google.com/share/...` URLs requiring an authenticated Google session —
WebFetch only ever returned the sign-in nav shell on both the original and
post-redirect URL. Could not extract their content; flagged honestly in the
deliverable rather than fabricated. Awaiting the owner to paste the text directly
if they want it folded in.

Researched all 10 repos in depth via 4 parallel research agents (not skimmed —
actual README content read, cross-checked against raw source where thin, one
hallucinated stat caught and discarded: a first-pass fetch invented "58k stars" for
Agent-Reach that a raw-README recheck found nowhere in the text). Synthesized
myself (not delegated) into docs/v2-vision.md:
- A compressed mechanism/hook/transfer table for all 10 repos, two marked "thin
  transfer" honestly (Agent-Reach, apple/container) rather than forced.
- A 6-pull psychology model (acute quantified pain, permanence/personalization,
  status/aesthetic signaling, visible trust boundaries, self-hosting/data-ownership
  culture, install-and-forget) mapped to specific Raphael levers.
- Token-economics section first (headroom-inspired): cache-stable injection
  ordering + pointer/retrieve for marginal lessons are flagged as the two
  highest-leverage, lowest-risk ideas — they strengthen the existing "net-lower"
  budget claim rather than just adding features.
- Luxury section: a developer-profile lesson category (hermes-agent's Honcho
  pattern, but through Raphael's existing chokepoint — no free-text side channel),
  a ritual digest rewrite, console visual-craft pass, trust-at-point-of-action,
  policy.js/eval marketing (not new code — surfacing what already exists).
- Reach section: AGENTS.md canonical + thin per-CLI wrapper files (career-ops
  pattern) flagged as the single highest-leverage idea for "industry standard" —
  widens delivery beyond the Claude-Code-only plugin without touching the
  chokepoint/curator/Atlas core.
- An explicit "what NOT to build" section re-confirming the no-embeddings and
  no-unbounded-agent-fetch rejections (open-notebook and Agent-Reach both make the
  case for the road not taken, independently of the earlier gstack audit).
- Phase 18 (10 milestones, all zero-new-network-surface) added to TASKS.md as
  PROPOSED, not started — this was a brainstorm deliverable, not a build request.

npm test still 415/415 (no code touched). Committing docs only.

## Round 4 — 18.2/18.6/18.8 finalized, gstack deep-dive, agent-roster plan, architecture audit

Owner gave four instructions in one message: (1) repropose 18.2 after actually reading
hermes-agent's code, not just its README; (2) decide 18.6 myself ("do as you see fit");
(3) drop 18.8; (4) clone gstack, read ALL its agents (not a prior session's narrower
scope) in depth, write findings to a durable doc, propose a Raphael agent-roster plan;
then act as a senior architect and audit both the existing architecture and everything
newly proposed for gaps/bad decisions; and record a testing standard (success + failure +
edge cases mandatory, going forward).

**18.2 repropose** — cloned NousResearch/hermes-agent, read `plugins/memory/honcho/
{client,session}.py` directly. Found and corrected a real error: the original proposal
had attributed a "corrections land as free text in CLAUDE.local.md" detail to Honcho —
that was actually headroom's `learn` feature, a different repo, conflated in the earlier
README-level pass. The real mechanism: Honcho is a THIRD-PARTY PAID SAAS (app.honcho.dev)
hermes-agent is a thin client of, not something it computes itself — "peer cards" (cheap,
pre-extracted facts) vs. "dialectic queries" (expensive, server-side LLM synthesis,
always backgrounded/prefetched), "conclusions" with NO client-side schema/scrubbing/
review, and `delete_conclusion()`'s own docstring says "use only for PII removal" — no
real correction workflow. Revised 18.2 in docs/v2-vision.md §4.1: atomic-facts-only
`category: preference` (never a synthesized narrative), same chokepoint + same retire/
decay lifecycle as every lesson (explicitly not Honcho's PII-only-deletion model),
built via the prefetch-in-background-thread pattern so it also chips at the existing
parked "inject latency ~390ms cold" item. TASKS.md 18.2 updated to match.

**18.6 decision (mine to make):** sequencing — pull forward to run 3rd (after 18.1's
correctness fix and 18.11's cheap defensive win), not last; scope — v1 ships the
canonical AGENTS.md generation + exactly ONE real CLI wrapper, not all of Codex/
OpenCode/Gemini CLI/Cursor at once, citing Raphael's own adopted no-gold-plating lesson
as the reasoning (prove one integration before building speculative breadth). Both
docs/v2-vision.md §5.1 and TASKS.md 18.6 updated with the decision + reasoning.

**18.8 dropped** — removed entirely from docs/v2-vision.md (§5.3 deleted, table row
struck through) and TASKS.md (marked dropped, not just unchecked).

**gstack deep-dive** — cloned garrytan/gstack (1,179 files, 54 skills). Given the
documented lesson that parallel-agent fan-out hits the rate limit on this exact repo
(session 10), read it ALL INLINE myself, not via subagents. Full line-by-line reads:
review/SKILL.md (1,853 lines) + its 7 specialist checklists, plan-eng-review (924
lines), plan-ceo-review (924 lines), cso/audit-phases.md (14-phase security audit),
learn/SKILL.md (953 lines), investigate's differentiated body, qa's setup/bootstrap
sections, ETHOS.md. Full YAML-frontmatter catalog for literally all 54 skills (name,
description, triggers, tools, declared gbrain context needs) so nothing was skipped
even where the step-by-step body wasn't read verbatim. Wrote docs/gstack-agents-audit.md
— corrects the terminology (gstack's "agents" are mostly slash-command skills with a
shared framework, not named personas the way Raphael's 10 are; the true persona-panel
analog is the review specialist family), documents the shared AskUserQuestion protocol
(one-decision-per-question, mandatory recommendation+pros/cons, a whole host-quirk
fallback ladder), the adaptive per-specialist hit-rate gating + red-team-runs-last +
pre-emit "quote the motivating line or get suppressed" verification gate, cross-model
"Outside Voice" with a hard-stated User Sovereignty rule, and reconfirms (now from
actual code, not just the prior session's inference) that gstack's `learn` memory is
unvalidated/unscrubbed — while also surfacing that "preference" is independently one
of gstack's own first-class learning types, converging with the revised 18.2 from a
totally separate source. Also documents what NOT to import (heavy telemetry preamble,
unvalidated confidence, /ship's auto-push+PR) and a named tension between gstack's
"Boil the Ocean" (always build the complete thing) and Raphael's own adopted
no-gold-plating lesson.

**Agent-roster plan** — read src/lib/agents.js in full first (10 agents, one shared
5-rule SPINE, 4 recipes) to ground every proposal in the real current code. Wrote
docs/agent-roster-v2-plan.md, Phase 19 in TASKS.md (19.1-19.6): a 6th spine rule for
one-decision-per-question discipline (19.1, prose-only); sharpened debugger/reviewer/
security missions with gstack's exact language — 3-strike rule, regression-test-must-
fail-then-pass, quote-the-line-or-suppress (19.2, prose-only); confidence-banded
findings for reviewer/security/debugger, extending confidence.js's existing lesson-
scoring discipline to live agent output for the first time (19.3); `raph guard scan
--skills` — gstack's CSO "skill supply chain" phase ported to guard.js, a threat class
Raphael doesn't cover today (19.4); opt-in cross-model outside voice for security-audit
+ pre-deploy ONLY, never auto-applied (19.5); per-agent outcome mining as the
Raphael-shaped version of gstack's adaptive hit-rate gating, flagged as needing its own
design pass, largest/last (19.6).

**Senior-architect audit** — wrote docs/architecture-audit-v2.md. Verified 3 real
claims in code before writing anything (not asserted): compile.js DOES re-validate
every lesson file at build time regardless of how it landed on disk (closes the
"agent bypasses the CLI" concern for structurally-invalid content) — but confirmed via
grep that neither validate.js nor the schema ties `status: active` to having passed
through approve.js, so a structurally-valid, hand-written active lesson would still
pass compile-time revalidation (§1.1, a real confirmed gap, framed as "needs an
explicit decision" not "is broken," since Bash-capable sessions already carry ambient
trust). Five gaps in shipped architecture (chokepoint validates structure not
provenance; "always net-lower" is true-by-cap not yet true-by-measurement until
18.1/18.10 ship; Raphael's own surface area risks the comprehension-debt problem it
sells against; global-brain governance is one-person-forever and doesn't scale past
today; Atlas's static-analysis blind spots aren't bounded/measured separately from its
efficiency win). Five gaps in the new proposals (18.2's preference lessons have no
decay/dispute policy — confidence.js's repetition-based model doesn't obviously fit a
fact that's true until silently reversed; 18.11 raises the cost of the MemoryGraft
attack class, doesn't close it — a sophisticated adversary can fabricate supporting
evidence too; 19.5 needs its own explicit invariant-#5e sentence plus a supply-chain
question about trusting an external codex binary, not an assumed fit under 5a; 19.6
risks a confidently wrong signal — transcript evidence can't distinguish "ignored
because wrong" from "ignored because busy" from "fixed differently"; 18.6 has no CI
story for when a host CLI's own convention format changes silently). Closed with what's
actually solid (chokepoint discipline re-validated 3x this session against different
sources; fail-open consistency; the deploy/spend/sign-in boundary holding across every
session; present-never-auto-apply as a real cross-subsystem principle, independently
convergent with both hermes-agent's absence of it and gstack's explicit ethos) and a
5-item "decide before building" punch list.

**Testing standard** — recorded as a new CLAUDE.md convention: every function/path
gets a success case AND a failure/error-handling case AND relevant edge cases; a
regression test must be shown failing without the fix and passing with it. Grounded
explicitly in gstack's investigate/plan-eng-review rules (independently confirmed) and
Raphael's own v0.2.0 CI miss (a check that looked green without proving what it
claimed) as the two sources for why this matters, not asserted from nowhere.

Research clones (hermes-agent, gstack) live outside this repo at
C:/Users/Mahesh/Desktop/Projects/_research/ — reference material only, not committed.
415/415 tests untouched throughout (docs-only session). Nothing built — all four new
docs (v2-vision.md updates, gstack-agents-audit.md, agent-roster-v2-plan.md,
architecture-audit-v2.md) are proposals awaiting the owner's go, same status as before.

## Round 3 — the Gemini research, in full (owner pasted the text directly)

The owner pasted the actual content of what turned out to be 3 reports (not 2): "Advanced
Optimization and Security Architectures in LLMs" (model-serving internals: LLMLingua-2,
KV-cache/MLA, TokenSkip, grammar-constrained decoding, a security threat survey incl.
slopsquatting + memory poisoning via AgentPoison/MemoryGraft, guardrail architectures,
loop-engineering's Trigger/Goal/Verifier/State), "Advanced Paradigms in AI-Assisted
Software Engineering" (token economics, gateway infra, GitClear/Faros AI/Veracode/Sonatype
telemetry, the Anthropic six-pattern interaction-style study), and "The Acceleration
Whiplash" (the deepest version — "Comprehension Debt" as a named term, ecosystem
decision-fatigue, the METR RCT where AI slowed experienced devs 19% on repos they knew
well). Read and analyzed one by one as the owner asked, not skimmed.

Before writing anything, checked 3 concrete claims against the actual code rather than
assuming (per this session's own established discipline):
- driver.js's `applyStageResult()` — confirmed it DOES implement a bounded circuit breaker
  (one escalated retry per stage via `retry_escalated`, then `d.status='failed'`, stops) —
  count-based rather than error-similarity-based, but real, not a gap.
- `REVIEW_TOOL` schema in adopt.js — confirmed the reviewer screen's risk kinds are exactly
  `prompt-injection`, `malicious-guidance`, `license`, `low-quality`, `other` — NO check for
  an unverifiable/fabricated outcome claim, i.e. no defense specifically shaped like
  MemoryGraft's attack pattern. Real, code-confirmed gap.
- lesson.schema.json's category enum — confirmed no `preference` category exists yet
  (validates the earlier round's developer-profile idea was correctly scoped as new).

Added docs/v2-vision.md §7 (renumbered old §7→§8): report-by-report summary, §7.1 "what
this confirms" (Ralph-Loop-shaped curator, driver's real circuit breaker, adopt gauntlet ≈
LlamaFirewall's layered defense — all validation, no new build), then 5 new items:
- §7.2 Comprehension Debt as Raphael's sharpest pitch — positioning, not code. The
  literature hands Raphael citable, industry-wide evidence for the exact problem it solves.
- §7.3 (flagged as best evidence-to-effort ratio in the whole doc) — add an
  `unverifiable-claim` risk kind to the reviewer screen, closing the MemoryGraft-shaped gap
  with a schema/prompt change to an existing gate.
- §7.4 A slopsquatting-defense lesson in the security pack (Sonatype's 27.8% hallucinated-
  dependency finding is a concrete, evidenced gap check against the existing 26-lesson pack).
- §7.5 Surface the mined "why" as its own field in injection, not just the "what" — nudges
  toward the interaction patterns (Conceptual Inquiry, Gen-then-Comprehension) the Anthropic
  study shows preserve skill, away from the ones (Delegation, blind Debugging) that erode it.
- §7.6 Name ecosystem decision-fatigue explicitly in marketing copy — the literature's own
  described anxiety is a direct argument for Raphael's one-governed-brain shape.

TASKS.md Phase 18 extended with 18.11-18.14 (18.11 = the memory-poisoning check, called out
as worth pulling forward regardless of final sequencing). Explicitly out of scope, stated in
the doc: grammar-constrained decoding, KV-cache/MLA, guardrail-model training — all
model-serving/inference internals Raphael doesn't own (it calls `claude -p` as a subprocess,
never controls token-level decoding). 415/415 untouched. Still a brainstorm — nothing built,
awaiting owner go, same as round 2.
