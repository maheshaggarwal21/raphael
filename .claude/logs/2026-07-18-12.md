# Session 12 — 2026-07-18

Owner directive: "now complete this 16.7 and then move to phase 11 and complete wrap up
for publishing online with a detailed documentation on how and when to use each skill and
a complete detail about raphael. update all the docs" + a 5-part owner handbook document
(pitch/features/user journey/interview Q&A/marketing posts + influencer outreach).

## 1. 16.7 LIVE adopt runs (COMPLETE — the owner's explicit go covers the spend + fetch)

- Cloned the sweep's two real sources into the scratchpad (read-only, MIT both):
  github.com/kpab/claude-fable-5-skills and github.com/Sahir619/fable-method.
- Reality check vs the plan: "karpathy-guidelines" and "fable-skills handover format"
  do NOT exist under those names in the real repos (the sweep's original fable-compiler/*
  URLs were wrong; names were approximate). Nearest real equivalents substituted:
  no-gold-plating (simplicity/surgical principles) and regrounding-summary (the
  handover/final-report format). Recorded in TASKS.md.
- 5 live `raph adopt <file> --skill` runs on the subscription provider, sequential/inline:
  1. fable-method core skill -> 5 lessons (TWIN CHECK, AUTH gate [security-critical],
     spec/tests/code contradictions, read-actual-sources, outcome-first reports).
  2. act-when-ready -> 1 skill draft (no lessons — reviewer read it as pure instruction).
  3. effort-calibrator -> 4 lessons (re-baseline effort on model upgrade, route effort
     by task class, max_tokens bounds thinking at high effort, max-effort overthinking)
     + 1 skill draft. Reviewer quality 3/3.
  4. regrounding-summary -> 1 lesson (private-vocabulary breaks readers) + 1 skill draft.
  5. no-gold-plating -> 4 lessons (diff maps 1:1 to request, inline single-call-site
     abstractions, validate only at boundaries, YAGNI [quarantined]) + 1 skill draft.
- License note: per-file adopt shows license "unknown" (detectLicense reads file content
  only; LICENSE sits at each repo root). Both repos hand-verified MIT before running;
  gate only blocks CODE adoption on unknown/copyleft, lessons/drafts unaffected.
- Curation: read the security + quarantined bodies in full (both clean, advisory,
  no embedded commands); approved 12 batch + 1 security --confirmed + 1 quarantined
  --confirmed. Zero rejects — no dupes vs the 43 active (trigram + my read).
- RESULT: brain 43 -> 57 ACTIVE; 4 new skill drafts in staged/skills/ (NOT installed);
  5 provenance records in `raph adopt list`; revoke available per adoption.
- 353/353 tests green (no code changed; boundary discipline).

## 2. Phase 11 publish wrap-up (this session, after 16.7)

(see later entries)

## Next
- Phase 11: README overhaul + docs/manual.md (every command: how/when) + LICENSE +
  CI workflow + package.json publish metadata + plugin docs refresh.
- Owner handbook doc (5 sections) at docs/owner/.
- npm publish + GitHub-public switch remain OWNER actions (sign-in/one-way).
