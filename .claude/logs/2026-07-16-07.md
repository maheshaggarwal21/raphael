# Session 2026-07-16-07

## Context
Owner: "go with your recommendation and do every part carefully and one by one
maintaining proper history and planner." Security floor KEPT (§11.11), hub static-first
(§11.12), CLI fetch approved + scoped (§0.6 amendment). Mid-session the owner added:
finish the current task, then stop, gather ALL remaining work into a new todo list,
then work that list. Done — list = 13.4, 13.5, auto-approve dial, 15.1-15.4 console
milestones, company-ops backlog.

## Architecture v1.1 (33feefe)
§0.6 amended (bounded user-initiated fetch) + CLAUDE.md invariant #5 matched; §11
gains decisions 10-12 (fetch allowed; security floor kept; hub static-first); new §13
(adopt gauntlet, provenance, auto-approve dial, deferred 13b) + §14 (console: local +
thin hub, one-engine-three-faces, localhost security). TASKS Phase 13 planned 13.1-13.5,
Phase 15 sequenced after 13.

## Phase 13 — SHIPPED end to end (5740df2, 1076622, a550ecc, aba2557, + this closeout)
- 13.1 provenance.js: append-only adoptions.jsonl (last-line-wins -> revoke is recorded
  history); license detect (SPDX + text, AGPL/LGPL before GPL — a real misread trap);
  family gate: unknown/copyleft BLOCK code adoption. Test bug caught: two ULIDs in the
  same ms share the timestamp prefix — prefix lookups must reach the random chars.
- 13.2 fetch.js: ALL of the §0.6 amendment enforced in one module. https-only with a
  loopback-http carve-out (own machine; makes the module testable with a real local
  server). Redirects re-checked per hop — no policy downgrade via redirect.
- 13.3 adopt.js: the six-layer gauntlet. Malformed reviewer verdicts BLOCK (fail-closed).
  Skill drafts branded "DRAFT — review before installing; a skill instructs agents".
  Revoke: candidates deleted, ACTIVE lessons retired (inspectable) + brain commit.
- 13.4 commands/adopt.js + cli wiring; --dry-run = read+license+estimate, zero calls,
  zero writes (tested).
- 13.5 LIVE dogfood on gstack setup-gbrain SKILL.md (the prior-art target named in the
  plan): see below.

## Live findings (the reason dogfoods exist)
1. TIMEOUT BUG: extraction over 60k chars blew provider.js's fixed 120s CLI timeout.
   Fix: a model call may carry timeoutMs; getModelCaller forwards it to the transport
   (test added); adopt calls use 240s. Distill untouched (small episodes).
2. The gauntlet WORKED live, layer by layer: scrub replaced 2 secret-shaped values
   pre-model; reviewer passed with an accurate verdict (quality 2/3, "no injection or
   malicious guidance"); the ephemera gate killed "use port 6543 not 5432" — a REAL
   volatile-literal catch on real material; chokepoint staged the rest.
3. Sandbox e2e: 5 staged -> revoke -> queue empty, ledger [REVOKED] with history.
4. Real-brain run: 8 lessons + 1 skill draft (secure-secret-collection-in-bash-workflows
   — genuinely good). Curated as owner-delegate: rejected 2 near-dupes the trigram
   dedupe missed (differently-worded same-lesson pairs — a known gap, reasons feed
   rejection memory), approved 4 batch + 2 security via --confirmed one-at-a-time.
   BRAIN 37 -> 43 ACTIVE. The adopt->review->approve flywheel is real.

## State
244/244 tests before closeout commit (+22 this session). Planner tasks #1-9 done;
next #10 auto-approve dial, then 15.1-15.4 console, then company-ops backlog (#15).

## Auto-approve dial engine — DONE same session (+6 tests -> 251/251)
- src/lib/autoapprove.js: dialLevel fails CLOSED (unknown -> off); standard = own mined
  lessons into the §9 auto tier (tier:auto tag, this-project scope when known, cap 30
  visible-stop); wide = + reviewer-passed adopted (daily cap 10, adoption id logged in
  every auto-approved event; adopt revoke retires them by source). Security + quarantined
  skipped at EVERY level; E-AUTOSEC chokepoint backstop asserted in tests from both sides.
- `raph auto` = the dial's verb (console settings will call it — no verb, no button);
  wired into distill (per-project grouping for scope) + adopt ([held] lines make every
  non-activation visible). Real brain smoke: auto-approve off, auto tier 0/30.

## Console 15.1 — server skeleton DONE same session (+4 tests -> 255/255)
- src/lib/web.js + `raph web`: 127.0.0.1-only bind, per-launch token, Host+Origin gate
  on EVERY request (hostile 403 even with the token — the token is not enough by
  design), 401 guard page with zero data on bare /, strict inline-only CSP + nosniff +
  no-store. /api/status = thin aggregation over the SAME lib functions the CLI uses
  (test asserts the shape stays CLI-derived). escapeHtml exported as the render rule
  for all 15.2+ pages. Real-brain smoke via the API: 43 active, dial off, 1 adoption.

## What's next
15.2 dashboard + review queue pages (approve/reject via the same verbs; heavyweight
security modal) -> 15.3 adopt inbox/lessons browser/activity feed -> 15.4 settings
(dial) + guard page + docs. Then company-ops backlog (#15). Planner tasks #10-11 done.
