# Session log — 2026-07-26-02 (session 16, part 2)

## Owner ask
"go on phase 21, fix all of them one by one" — then, after the two flagged
items: "do the two things, then continue and stop after everything is complete."

## What was done
Phase 21 (remediation of the fresh-eyes audit) end to end, plus the two flagged
items. Ritual honoured at every boundary: npm test green, commit, push.

### The two things
1. Deleted the 10 orphaned atlas caches the 21.4 rekey left behind, incl. a
   64.5MB `Mahesh.json` (an atlas accidentally built over the home directory).
   62.8 MB freed; `raph atlas where` verified working afterwards.
2. The release: version 0.5.0 across package.json + plugin.json + marketplace.json,
   CHANGELOG written, tag + push. `npm publish` remains the owner's action.

### P0 (all 10)
21.1 E-LIMIT family — ONE detectLimit() replacing a regex that ran over the
     model's own successful output in three places. Also made curateStaged
     limit-safe (gate-or-rollback the partial batch, report `limited`, exit 4).
21.2 Recall invariants made structural filters; match.js de-NUL'd (git treats it
     as text again — the NULs had broken grep during the audit itself);
     globToRegex rewritten single-pass, `?` fixed, space-sentinel bug gone.
21.3 fetch.js SSRF: IP-literal layer + guarded dns lookup (closes rebinding);
     the loopback carve-out follows the ORIGIN, never a redirect target.
21.4 Atlas keyed by basename+path-hash with a verified root; three duplicate
     keyings collapsed to one; `raph lint` scopes staleness to the linted project.
21.5 Hot path: lazy ajv, stat fast-path, digest marker, atlas size cap + nudge
     reorder. MEASURED end-to-end hook ~390ms -> ~137ms.
21.6 academy readState: corrupt != missing (quarantine the bytes, throw).
21.7 Near-dup gate on the plain dial; console adopt now uses curateStaged.
21.8 curator counter_indications string fix.
21.9 Chokepoint scans parsed data too; four measured scrub gaps closed; two
     false positives fixed; SECRET_RULES table-driven; E-BASE64 finally covered.
21.10 guard -z listings + warn on unreadable staged blobs.

### P1/P2
21.11 Eval honesty (the big one): diff-based checkers + the no-agent meta-test,
      S15/S31 reverse bias, printed Wilson + Newcombe intervals with a sample
      floor, the declarative canary arm wired with an injectable executor, and
      eval.js's drifted copy of the per-prompt selector deleted.
21.12 18.7 skill lint + 18.10 effort router wired (both had NO production caller).
21.13 Generated plugin agents/recipes byte-pinned to agents.js.
21.14 CLI exit codes (70 crash vs 2 policy) + router tests + --max-episodes fails
      closed; events rotation + windowed reads; latency_ms + latencyHealth;
      console CSP nonce / real 413 / adopt lock / shared tier count / token
      stripped from URL; atlas extraction blind spots; honest bench label; pulse
      lock heartbeat + atomic steal; tool-correlated error-fix; envelope forging;
      quarantine timestamp; freshness false positives; AGENTS.md ranked
      selection; doc + number drift; npm files whitelist (800KB -> 345KB).

## Bugs I made and caught
- **Wrote a vacuous test.** The first YAML-escape regression passed instantly —
  the heredoc collapsed the escape into the literal character, so it tested the
  EXISTING raw scan. Probed the real behaviour (ok:true with a zero-width space
  in data.title), rewrote the test to build the escape at runtime, proved it red.
- **Hit the backtick trap again** — `git commit -m "...`tried`..."` was command-
  substituted by bash. Harmless (not a real command) but the message was
  corrupted. Amended with a quoted heredoc and BROADENED the CLAUDE.md rule: it
  named node -e/bash -c, and I got caught by git commit -m. It now covers any
  double-quoted shell string and gives the heredoc pattern.
- Attached a property to an array (`scanStaged`) — a pre-existing test caught it;
  changed to `{results, unreadable}`, matching its sibling scanTracked.
- Assumed a corrupt academy state would exit 70; the command handles it cleanly
  at exit 1. Test premise was wrong, not the code — rewrote the assertion.

## Findings that turned out to be MY OWN earlier work
- 18.7 and 18.10 were recorded as shipped in session 15 with no production
  caller at all. Reported plainly and fixed rather than quietly wired.

## State at close
- 603 tests (from 499), all green. `raph doctor` healthy. 43 verbs.
- Version 0.5.0 tagged; `npm publish` is the owner's switch (registry: 0.4.0).
- docs/codebase-audit-2026-07-26.md carries a REMEDIATED banner; TASKS.md Phase
  21 marked complete with a "deliberately not done" list (walker/arg-parser
  consolidation, mine/distill extraction, similarity recalibration, A9).

## What's next
- Owner: `npm publish` for 0.5.0.
- The four deferred items above, each with its reason recorded.
