# Session 2026-07-17-09

## Context
Phase 15 (console) closed last session at e62229a. This session = planner #15,
the company-ops backlog (Phase 12/14). First move: dependency-ordered the Phase 14
plan in TASKS.md — data substrate first (14.1 portfolio -> 14.2 weekly report ->
14.3 console Company tab), then the driver stack (14.4 model policy -> 14.5
scheduler + autopilot), then the meta layer (skills factory, agent-maker, optimizer).

## 14.1 — portfolio registry SHIPPED (+3 tests -> 262/262)
- Finding that shaped the design: NO lesson in the real brain has a non-empty
  scope.projects, so deriving "lessons written back per project" from the index
  would report 0 for every project. The honest source is an explicit record:
  `raph academy checkpoint --tests N --lessons N` (E-ACADEMY on junk; shown in
  renderStatus). Backfilled repo-keeper 41/3, onedesk 64/4, assay 59/4.
- src/lib/portfolio.js: buildPortfolio({states, events}) pure (per project:
  status, milestone progress, recorded tests/lessons or an honest null,
  recall = injected-event tokens where e.project matches, boundary reason,
  next action suppressed for done projects; newest-updated first) +
  readPortfolio() disk wrapper + renderPortfolio() table.
- `raph portfolio [--json]` = a thin printer (cli.js + help updated).
- Dedup prereq: readEvents() moved into lib/events.js next to logEvent —
  commands/stats.js, commands/why.js, and lib/web.js all imported the same
  copy-pasted loop; all three now use the one function.
- Live on the real brain: table shows 3 done projects, 164 recorded tests,
  11 lessons written back, 0 recall tokens (true — the hooks have not fired
  inside those builds yet; stats said the same).

## 14.2 — `raph report weekly` SHIPPED (+3 tests -> 265/265)
- src/lib/report.js: computeWeekly() pure with `now` as a parameter (every
  window testable). Sections: build activity (checkpoint notes in-window),
  brain changes (approve/auto/reject/suppress/adopt funnel), recall cost,
  retrieval miss (deliberately ALL-TIME — dead weight is dead weight in any
  window), adoptions (latest ledger ts in-window), next/owner asks (non-done
  projects, boundary reasons rendered as "OWNER: ...").
- readWeekly() disk wrapper + renderWeekly() + `raph report weekly [--days N]
  [--json]` thin printer; cli.js + help.
- Live on the real brain, all sections populated with the week's TRUE story:
  3 builds active (8/6/6 notes), 43 approved + 2 rejected, 1 adoption
  (gstack skill: 8 lessons + 1 draft), 1,650 recall tokens / 5 sessions,
  39/43 lessons never fired (expected pre-RUN; the report says so plainly).

## 14.3 — console Company tab SHIPPED (+1 e2e -> 266/266)
- Routes: GET /api/portfolio (= readPortfolio verbatim), GET /api/report[?days=N]
  (= readWeekly; non-positive-integer days -> 400 E-WEB, same validation as the
  CLI flag). Company tab (8th) renders the portfolio table — boundary rows in
  red as OWNER asks, next-action rows muted — plus every weekly-report section.
- Template-literal trap respected: zero backticks added inside the page.
- e2e: seeded academy project + events through the REAL server; asserted the
  API payloads equal the engine output (tests/lessons/recall/next), 400s on
  days=abc/-2, and the page ships tab-company.
- Live browser smoke on the real brain: clicked the Company tab, the true
  portfolio (164/11) + weekly report rendered; zero console errors.

## 14.4 — model policy table SHIPPED (+4 tests -> 270/270)
- src/lib/policy.js: POLICY = 14 task kinds (the pipeline stages + cross-cutting
  shapes) -> {agent, model, effort, escalate, why}. Cheap -> strong made law:
  opus appears ONLY as an escalation target (debug), never first-pass — a test
  asserts no entry ships model:'opus'. distill keeps model:null (CLI default;
  containment is the guarantee, not the tier) and renders "(cli default)".
- One source of truth kept: checkRosterAlignment() — a policy entry naming an
  agent must prescribe the roster's model unless the roster says 'inherit'
  (developer), in which case the policy IS the concrete answer. Tested, so the
  table and agents.js can never drift apart silently.
- resolvePolicy(kind, {escalated, overrides}) validates models/efforts (E-POLICY
  with the valid list); escalating a kind with no escape hatch throws (caller
  bug). resolveForAgent(slug) is what the 14.5 driver will hold.
- `raph policy [<kind>] [--escalated] [--json]` thin printer (cli.js + help).
- provider.js: buildCliArgs + callModelCLI now accept `effort` -> `--effort`
  (flag confirmed on the installed CLI v2.1.168); absent when not passed, so
  every existing call path is unchanged. Regression test both ways.
- Live smoke: full table renders; `raph policy debug --escalated` = opus/high;
  unknown kind = E-POLICY listing valid kinds, exit 1.

## 14.5 — autopilot driver + limit-aware scheduler SHIPPED (+4 tests -> 274/274)
- src/lib/driver.js, split exactly like eval (pure harness / one spending runner):
  - State machine: initDriver (validates every pipeline kind via resolvePolicy —
    so a "deploy" kind is IMPOSSIBLE, E-POLICY at init), nextAction (brief feeds
    stage 0; each stage's output is the next stage's input; policy resolved per
    stage, escalated on a retry), applyStageResult (ok -> advance; fail -> ONE
    escalated retry when the policy has an escape hatch, else driver=failed).
  - Driver state lives INSIDE academy state.json -> RESUME.md/resume.ps1/Startup
    launcher all carry it with zero new plumbing. State is written BEFORE every
    spawn, so any interrupt resumes exactly.
  - makeStageRunner: real `claude -p`, tools ON + acceptEdits (stages write real
    files), workspace cwd, API keys stripped (subscription-forced), per-stage
    --session-id; interrupted stage reruns with --resume <id>. E-LIMIT detection
    reuses provider.js isLimitMessage/parseResetInfo.
  - drive(): the loop. E-LIMIT -> writeState + recordLimit(reset time) + exit 4;
    rerunning `drive` clears blocked-limit and RESUMES the same stage's session
    (tested). Completion -> recordBoundary (deploy/publish = owner) + blocked.
- `raph academy drive <project> --brief "..."|--brief-file <f> [--pipeline "a,b"]
  [--dry-run] [--max-stages N]`; dry-run renders the full plan, spends nothing.
- Tests: full-loop chaining w/ fake runner; limit interrupt -> resume SAME session;
  debug fails twice -> sonnet then opus(escalated) then failed; prompts carry
  boundary rules + roster missions; args (session-id/resume/no --tools); renderPlan.
- Bug found by the first test run: drive() reads DISK state — the test had only
  mutated memory. Fixed the test (writeState before drive); the code was right.
- LIVE (one real stage, subscription): `academy drive livekit --pipeline plan`
  -> real claude -p ran the Planner mission, wrote spec.md into the sandbox
  workspace, 541 tokens recorded in state.json, boundary recorded on completion.
  Live run exposed a wording flaw (boundary said "deploy-prep checklist" on a
  pipeline without that stage) -> message now conditional.
- Phase 12 items this closes: autopilot driver, limit-aware scheduler, session
  resume per stage, boundary-in-code (+ 14.4's model policy). Still open in
  Phase 12: mining wired into the loop, ON/OFF tokens-per-task, backlog w/ owner.

## What's next
Phase 14 meta layer: skills factory -> agent-maker (proposals only) -> optimizer
loop -> self-upgrade rule. Also parked: 13b read-understand-patch (driver now
exists), Phase 10 RUN (calendar), Phase 11 publish (owner timing).

## Research sweep -> Atlas plan (owner ask, after 14.5)
- Owner delivered two asks: (1) the AWARENESS problem — devs stop knowing their own
  AI-built code; Raphael should carry an "error type -> where to look" map; (2) deep
  research (git clone + analyze) on ~11 repos + 25 screenshots + a PDF, claiming
  70-80x token cuts, then a detailed upgrade plan (bigger than raph adopt).
- Cloned + read 13 repos. URL corrections: the four "fable-compiler/*" repos don't
  exist; real sources = kpab/claude-fable-5-skills (act-when-ready, effort-calibrator,
  markdown-memory) + Sahir619/fable-method; oliwoodman/fable-skills found + included.
  Screenshots (read as images after .jfif->.jpg rename — Read dumps binary for .jfif)
  = two carousels: Graphify+Obsidian+Claude Code story; okaashish repo list
  (context-engineering-intro, vibekit, claude-code-settings, ...). PDF = 10-tool
  listicle (Dify, OpenHands, ...), context only.
- THE finding: graphify (Graphify-Labs) — deterministic zero-LLM graph build
  (tree-sitter AST), EXTRACTED/INFERRED/AMBIGUOUS edge confidence, Leiden communities,
  god nodes, SHA256 incremental cache, query/path/explain + MCP + PreToolUse nudge
  hooks, Obsidian export w/ source backrefs. Audited benchmarks: 71.5x fewer tokens
  PER QUERY on a 52-file corpus (scales with size; ~1x tiny corpora), $0 build cost,
  +11pt answer quality on a 1M-LOC repo vs grep. The 70-80x claim is real, with the
  honest caveat that it's per-query after a free build.
- Also mined: fable-method (fit gate, TWIN CHECK — measured 1/5->5/5 fix completeness,
  AUTH gate = our invariant #3, artifact gate, eval-backed rules), kpab skills
  (markdown-memory = independently-evolved Raphael — strong validation + the
  retire-wrong-lessons gap), fable-advisor (architect pattern: expensive model emits
  fewest tokens), obsidian-mind (progressive disclosure ~2K start injection, code-owns-
  environment/agent-owns-content), obsidian-second-brain (OKM freshness: every fact
  timeless/dated/pointer + lint). NOT adopted: pxpipe (image-context proxy — lossy,
  silent confabulation, invariant #5 tension), tree-sitter/embeddings (dep rule;
  graphify proves embeddings unnecessary), hosted memory, vibekit (for now).
- Deliverable: docs/atlas-upgrade-plan.md — full analysis + Phase 16 "Atlas" proposal:
  16.1 deterministic project graph lib (zero tokens), 16.2 `raph atlas where` error
  router (the owner's awareness ask, verbatim), 16.3 query-first wiring into inject/
  hooks/driver, 16.4 honest token bench, 16.5 Obsidian-compatible export, 16.6 OKM
  freshness lint + lesson retirement, 16.7 adopt runs over the sweep's skills.
  TASKS.md Phase 16 added (PROPOSED). Both asks solved by the same engine: the map
  that tells a human where to look is the graph that saves the agent 10-70x tokens.

## What's next
Owner go on Phase 16 (Atlas) vs Phase 14 meta layer first. Atlas 16.1+16.2 recommended:
it is the research sweep's payoff and pure zero-token Node work.

## Two-repo re-check + 16.1/16.2 SHIPPED (+5 tests -> 279/279)
- Owner asked to re-verify andrej-karpathy-skills + kepano/obsidian-skills, then
  continue on my recommendations. Full read done. Plan updates: karpathy's 4
  principles named in 16.7; kepano's defuddle IDEA (zero-dep HTML->text cleanup
  in adopt fetch, fewer reviewer tokens) folded into 16.7; JSON Canvas 1.0 +
  Obsidian wikilink rules now specify 16.5's export formats.
- 16.1 src/lib/atlas.js: pure deterministic graph — collectFiles (bounded walk),
  extractFile (exports/imports/call-candidates/E-codes for JS+TS+PY+docs),
  buildAtlas (import resolution EXTRACTED; calls INFERRED 0.65-0.95 by evidence,
  multi-exporter no-import = AMBIGUOUS, surfaced in the report; tests/raises/
  mentions/uses; degree; top-dir groups), scanProject (SHA256 per-file cache),
  renderAtlas/renderDigest, whereQuery/pathQuery/explainQuery/resolveNode.
  paths.js + p.atlas() (brain/atlas/). Command src/commands/atlas.js +
  cli registration; where/path/explain/digest auto-build on demand (free).
- LIVE smoke on raphael (146 files, 459 nodes, 1481 edges, 0 tokens) caught 4
  real bugs the unit tests missed:
  (1) E-code origins missed errors.push({code:'E-SCHEMA'}) lines (no `throw` on
      the line) -> origin = quoted code in a code file, not throw-line only;
  (2) my own test fixture outranked the REAL raise site -> query-time weights
      (test x0.4, doc x0.6) — the graph stays true, ranking gets honest;
  (3) `path` routed through pkg:node:fs (hub shortcut) -> pkg nodes are
      endpoints, never waypoints;
  (4) THE CACHE BUG: --refresh after the extractor fix reused every stale
      extraction (hash-keyed cache, semantics changed) -> ATLAS_VERSION stamped
      into the doc; scanProject discards a cache from any other version.
  All four have regression tests. Final live check: `raph atlas where "E-SCHEMA"`
  -> src/lib/validate.js #1 "error text origin"; path candidates.js->validate.js
  clean 2 hops; second --refresh = 146 reused / 0 extracted.
- Next: 16.3 query-first wiring (digest into inject budget + PreToolUse nudge +
  driver stage prompts).
