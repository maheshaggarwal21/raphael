# Session 2026-07-14-01

## Context
Continued from session 01 (compacted). Two owner asks this session:
1. Continue the interrupted "unique project ideas" research, and add a new filter:
   projects must be worth selling / attract users (mobile = Play-Store-worthy, AI agent
   must get users).
2. A security PDF was attached (`emergent-security-prompts (1).pdf`) — study it and apply
   additions to Raphael.

## What was done

### Task 1 — project ideas (delivered, awaiting owner pick)
- The interrupted workflow (wf_558bacf6-ac7) had 4 of 6 inventor agents finish before the
  session limit. Recovered all 12 cached ideas from journal.jsonl instead of re-running
  agents (avoids another limit hit).
- Re-screened them through the new sellability filter and presented a 3-per-genre shortlist
  in chat with a recommended trio:
  - Web: **Grasshopper** (on-device browser audio mastering) ★, Hum (media authenticity /
    anti-deepfake), Palimpsest (self-eroding team canvas).
  - Mobile: **Vanish** (real-time on-device video object removal) ★, Stethophone (diagnose
    machines by sound), Weatherbone (phone-barometer migraine forecasting).
  - AI-agent: **Palpate** (MCP runtime behavioral-diff tool) ★, Ablate (deterministic
    record-replay for agent runs), Fenceline (least-privilege agent sandbox).
- Waiting on the owner to pick one per genre to lock the Academy backlog.

### Task 2 — security additions (built, tested, green)
Studied the PDF (extracted text with PyMuPDF — installed it; the repo's raw-stream
extractor couldn't decode the subset fonts). It is 5 pro audit prompts: Gitleaks (secrets),
Bearer (PII flow), ECC (pre-deploy), Trail of Bits (deep logic), ECC (attacker review).
Saved the extracted text to scratchpad (sec.txt). Applied three additions:

1. **Curated security starter pack** — `src/lib/security-pack.js` (19 atomic lessons) +
   `raph pack [list | add security [--dry-run]]` (`src/commands/pack.js`). Cold-start value:
   a fresh brain is empty; this seeds it with the mistakes that cause most breaches. Every
   lesson goes through the ONE chokepoint (writeCandidate → validateLesson), URL-free,
   declarative voice, category security + tier curated + status candidate, so it lands as a
   REVIEWABLE candidate on the heavyweight security-approve path (never machine-activates).
2. **+3 eval scenarios** from the doc — S20 IDOR, S21 security-headers, S22 client-price —
   pure fixture+checker, wired into `src/eval/scenarios.js`. Verified all three defending
   lessons FIRE on their prompts (no retrieval miss) via `raph eval run --dry-run`.
3. **`security-audit` recipe** (the 5 checks, in order) added to `src/lib/agents.js`;
   pre-deploy recipe now runs it first. Regenerated `plugin/recipes/` (build-agents.mjs →
   10 agents + 4 recipes). Ties to the Academy: a build isn't deploy-ready until it passes.

## Tests
182/182 green (was 172; +7 security-pack, +3 eval scenarios). Ran full `npm test`.
- New: `test/security-pack.test.js` (7), 3 scenario-checker tests in `test/eval.test.js`,
  updated `test/agents.test.js` (RECIPES 3 → 4, asserts security-audit ships).

## Decisions
- Pack ships as CANDIDATES, not active lessons. Honors the invariant that security lessons
  get human eyes; the "instant value" story is one `raph pack add security` + review away.
- Tool names are attribution only (no URLs) — invariant #3 kept; the chokepoint would have
  rejected any URL anyway (proven by the no-URL test).
- Reused the interrupted workflow's cached output rather than spawning fresh agents, to stay
  clear of the session limit.

## Bugs / gotchas
- `ideas_cache.txt` accidentally written to the repo root by a PDF-extraction script (bash
  CWD was the repo at that call). Removed it before committing.
- PDF was subset-font + custom CMaps; the naive stream extractor returned glyph codes.
  Installed PyMuPDF (pip) to get clean text. (Anaconda python present; no PDF lib by default.)

## Continuation (same session) — security gaps + Academy kickoff + first build

### Security pack completed
Filled the 7 coverage gaps the owner asked about: XSS/output-encoding, data-deletion,
debug/test-endpoint removal, env-var startup validation, DB TLS+creds, internal-file
exposure, Supabase-anon-key-needs-RLS. Pack now 26 lessons, all clean through the chokepoint
(commit 95eec8f). Confirmed the PDF extraction matched the owner's pasted text 1:1.

### Ideas: two rounds rejected, owner proposed their own three
My "creative" ideas were rightly called imaginary/unrealistic. Owner proposed: (1) web =
business+personal ops+finance+advisor; (2) app = offline photo grouper by faces; (3) AI
agent = a GitHub repo-lifecycle manager (freshness + doc-sync + security auditor). Expanded
all three in docs/academy/backlog.md; decided to build #3 ("Repo Keeper") first — CLI-shaped,
head-lessly verifiable, dogfoods Raphael, stays inside the autonomy boundary.

### Academy machinery (Phase 12 START)
Built the durable checkpoint/resume system so an autonomous build survives Claude limits AND
PC restarts with no owner input:
- src/lib/academy.js + commands/academy.js: `raph academy start|status|resume|checkpoint|
  boundary|limit|list`. State in ~/.raphael/academy/<project>/state.json (atomic). 7 tests.
- .claude/academy/RESUME.md (runbook a fresh session reads) + AUTORESUME.md.
- Auto-resume: .claude/academy/resume.ps1 (guarded: only fires while in-progress/blocked-limit,
  30-min throttle, visible window, logs) + a no-admin Startup-folder launcher (schtasks needs
  admin, unavailable here). Layer 1 (checkpoint) reliable; Layer 2 (logon launch) best-effort.
- Commits: e2b6c3b (machinery + backlog), 2c345bd (auto-resume).

### Repo Keeper build (workspace: Desktop/Projects/repo-keeper, local git, NO push)
- M1 (commit b304a91): scaffold + shared scanner core (scanRepo: stack, structure, manifest,
  vital signs, injectable git). `keeper scan`. 7 tests.
- M2 (commit 6d8dd28): freshness engine + `keeper freshen`. EOL runtime (open floor vs pin —
  fixed a real false positive where ">=18" was treated as a hard pin), vulnerabilities (npm
  audit), outdated deps (npm outdated, major/minor/patch), missing scaffolding → verdict +
  SAFE ordered update plan. 18 tests. Self-run flagged repo-keeper's own missing lockfile →
  fixed it (dogfooding). Wrote the engines-floor lesson back to the brain (candidate).
- Checkpoint now at M3 (Doc-Sync). M4 = Security Auditor (reuses the pack), M5 = integration.

### Used Raphael as designed
`raph search` (brain empty — nothing approved yet; honest cold-start), `raph map` (built a
repo-keeper map), `raph note` (wrote back the engines-floor lesson). Flywheel starts once
candidates get approved.

## Next
- Continue Repo Keeper: M3 Doc-Sync → M4 Security Auditor → M5 integration + self-run. All
  resumable from `raph academy status repo-keeper`.
- Owner action pending at the end: any GitHub push / publish (autonomy boundary).
- Still open (unchanged): Phase 9 plugin packaging, Phase 7 `raph init --guard`.
