# Session log — 2026-07-13 (01): scaffold + foundation

## What happened
- Initialized the git repo and the Node project (`raphael-brain`, ESM, deps: js-yaml + ajv only).
- Built cutline step 1 (Phase 1 in TASKS.md):
  - `src/schemas/lesson.schema.json` — canonical strict schema v1 (trust tiers curated/user/auto included so §9 distribution needs no migration later).
  - `src/lib/ulid.js`, `frontmatter.js`, `files.js` (atomic writes), `paths.js` (RAPHAEL_HOME override for tests/sandboxing).
  - `src/lib/scrub.js` — secret scrubber: 10 typed rules + Shannon-entropy scan.
  - `src/lib/validate.js` — THE chokepoint: schema + no-URL + deny-list + invisible-unicode + base64 + secret scan + `E-AUTOSEC` (auto tier can never hold security lessons) + imperative-voice quarantine.
  - CLI: `raph init` (tree + config + brain git repo + pre-push guard hook), `raph status`, `raph validate`.
- Tests: 24/24 green (`npm test`, node:test, no test framework dep).
- Smoke test in sandbox brain: init → seed 2 lessons (1 clean, 1 poisoned) → status → validate. Poisoned lesson correctly failed with E-URL + E-DENY; exit code 1.

## Bugs found and fixed during the session
1. `node --test test/` doesn't resolve a bare directory on Windows → switched to glob `"test/*.test.js"`.
2. Scrubber cascade: the kv-secret rule re-matched our own `<SECRET:...>` placeholders (word "SECRET" + colon looks like a key=value pair) → fixed with a `(?<!<)` lookbehind.
3. Entropy scanner flagged Raphael's own ULIDs (26 random base32 chars) as secrets → exempted `les_/ev_/prj_/mch_` id patterns; safe because the base32 charset can't encode arbitrary secrets.
4. Git Bash `/tmp` maps to `C:\Program Files\Git\` when passed to Node → use real Windows paths for RAPHAEL_HOME in tests/smoke runs. Worth remembering for the eval harness.

## Decisions made
- Strict schema (`additionalProperties: false`) — unknown fields are rejected at the chokepoint, security-preferred.
- Status enum is the superset of both docs' lifecycles: candidate/active/probation/retired/archived/quarantined.
- Imperative voice ("you must...") = quarantine (human must look) but not hard reject; injection-shaped text (deny-list) = hard reject + quarantine.

## Second pass (same day) — Phase 1 completed
- Owner suggested categorizing lessons by agent role. **Partially adopted**: added
  `scope.agents` (enum of the 8 roles, empty = all) as a retrieval FILTER, not the
  primary category — most lessons serve several roles (webhook idempotency matters to
  developer, reviewer, AND debugger), so agent-as-folder would force duplicates.
  Each agent retrieves only its slice → sharper injection, fewer tokens, searchable
  (`raph search --audience debugger` later). ARCHITECTURE.md §2 and §4 updated.
- Built `raph doctor`: node/git/brain/config/pre-push-hook/lesson-validity/transcript
  checks, PASS/WARN/FAIL with one-line fixes. Verified against the real machine.
- Built evidence records: `evidence.schema.json` + `writeEvidence()`/`readEvidence()`
  — excerpts auto-scrubbed at write time (aws-key test proves it), yy/mm sharding,
  atomic writes, E-EVIDENCE coded errors.
- Added `.gitattributes` (eol=lf) to stop CRLF warnings.
- Tests: 30/30 green. Committed in logical commits; GitHub repo created (private
  until launch, per the open-source-after-testing plan).

## Third pass (same day) — Phase 2 complete
- Ran a 4-agent parallel build (transcripts+consent, episodes, ledger, note). The
  ledger agent finished (9/9 tests); the other three hit the session usage limit —
  transcripts/config landed complete with tests, episodes.js landed complete WITHOUT
  tests, note/candidates never started. Finished the remainder by hand: episodes
  tests, candidates.js (chokepoint-enforced writer), note command + tests, mine.js
  orchestrator + CLI wiring.
- Bug found + fixed: async-unsafe `withSandbox` in note.test.js — sync try/finally
  around an async fn restored RAPHAEL_HOME while the test still ran, and restoring
  `undefined` into process.env coerces to the string "undefined" (created a junk
  `undefined/` dir in the repo). Fixed with async/await + delete-on-undefined;
  hardened evidence.test.js the same way. All 4 test failures were this one bug.
- End-to-end verification on REAL data (fake USERPROFILE + backdated transcript copy,
  sandbox RAPHAEL_HOME): mined this project's own build session → 585 events,
  4 error-fix episodes (including the orchestrator's own out.designs crash and
  read-before-write error from earlier today). Second run = clean no-op (ledger).
  The AWS test key present in the real transcript came out scrubbed (0 raw, 2
  placeholders). `raph note` + `raph status` verified in the same sandbox.
- 80/80 tests green.

## Fourth pass (same day) — Phase 3 complete, built solo per owner instruction
- New modules: similarity.js (trigram Jaccard), slug.js (extracted from note.js),
  model.js (the ONLY network surface: direct Messages API via fetch, forced single
  tool = structured output + true zero-tool containment, 429/5xx retries, 90s
  timeout), distill.js (pipeline), commands/distill.js (CLI with cost gate).
- Key design decision: G1 is structural, not a check — the model only proposes
  advisory text; the pipeline writes the evidence record itself from the episode it
  fed in. Fabricated provenance is impossible rather than detected.
- Rubric pass rule: counterfactual ≥2 AND actionable ≥2 (deviates from the design
  doc's avg≥2 on purpose: a 3/1 split is vivid-but-vague noise).
- Test-first bug: my "distinct" cap-test candidates were template-copies of each
  other and the in-run dedupe correctly caught them — the test was wrong, the gate
  was right. Rewrote the fixtures to be genuinely different.
- 96/96 tests green. Dry-run verified against the 4 real episodes mined earlier
  (~6.5k token estimate on Haiku). No ANTHROPIC_API_KEY on this machine, so the
  live-API path remains smoke-untested — first thing to verify when a key exists:
  RAPHAEL_HOME=<sandbox> raph distill --yes.

## Fifth pass (same day) — Phase 4 CLI substrate complete
- New: lib/queue.js (stable severity-sorted queue, ref resolution by number/slug/
  id/hash, needsConfirmation), lib/events.js (extracted from distill), lib/
  braingit.js (best-effort auto-commit of the brain repo), commands queue/show/
  approve/reject; findEvidence(id) added to evidence.js for show --provenance.
- Security rule from the adversarial review is now CODE: security-category and
  quarantined candidates cannot be batch-approved and require --confirmed after a
  full-body read. Batch calls approve the safe items and refuse the risky ones.
- approve = validate-on-write (a candidate that stopped passing validation cannot
  activate), slug-collision guard, friendly no-op for already-active refs,
  auto-commit "approve: N lesson(s)" in the brain repo. reject writes the tombstone
  shape distill reads; integration test proves reject → auto-suppression of
  lookalikes end to end.
- Decision: dropped `raph promote` as a separate verb — approve by slug/number
  covers the file-native flow; fewer verbs to document and test.
- 104/104 tests. Smoke: approved the session's own "Git Bash /tmp" note into the
  sandbox brain (first ACTIVE lesson ever), rejected a fresh note with --reason,
  verified tombstone + brain git log.

## Next session
Phase 5 (index + injection): compiled.json builder (hash-verified), deterministic
matcher/scorer, SessionStart + UserPromptSubmit hooks with token budgets, raph why,
events telemetry for injections. Live-API distill smoke still pending a key.
