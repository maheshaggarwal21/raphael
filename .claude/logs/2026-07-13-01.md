# Session log — 2026-07-13 (01): scaffold + foundation

## What happened
- Initialized the git repo and the Node project (`raphael-brain`, ESM, deps: js-yaml + ajv only).
- Built cutline step 1 (Phase 1 in TASKS.md):
  - `src/schemas/lesson.schema.json` — canonical strict schema v1 (trust tiers curated/user/auto included so §9 distribution needs no migration later).
  - `src/lib/ulid.js`, `frontmatter.js`, `files.js` (atomic writes), `paths.js` (RAPHAEL_HOME override for tests/sandboxing).
  - `src/lib/scrub.js` — secret scrubber: 10 typed rules + Shannon-entropy scan.
  - `src/lib/validate.js` — THE chokepoint: schema + no-URL + deny-list + invisible-unicode + base64 + secret scan + `E-AUTOSEC` (auto tier can never hold security lessons) + imperative-voice quarantine.
  - CLI: `raph init` (tree + config + brain git repo + pre-push guard hook), `raph status`, `raph validate`.
- Tests: 24/24 green (`npm test`, node:test, no test framework dep).
- Smoke test in sandbox brain: init → seed 2 lessons (1 clean, 1 poisoned) → status → validate. Poisoned lesson correctly failed with E-URL + E-DENY; exit code 1.

## Bugs found and fixed during the session
1. `node --test test/` doesn't resolve a bare directory on Windows → switched to glob `"test/*.test.js"`.
2. Scrubber cascade: the kv-secret rule re-matched our own `<SECRET:...>` placeholders (word "SECRET" + colon looks like a key=value pair) → fixed with a `(?<!<)` lookbehind.
3. Entropy scanner flagged Raphael's own ULIDs (26 random base32 chars) as secrets → exempted `les_/ev_/prj_/mch_` id patterns; safe because the base32 charset can't encode arbitrary secrets.
4. Git Bash `/tmp` maps to `C:\Program Files\Git\` when passed to Node → use real Windows paths for RAPHAEL_HOME in tests/smoke runs. Worth remembering for the eval harness.

## Decisions made
- Strict schema (`additionalProperties: false`) — unknown fields are rejected at the chokepoint, security-preferred.
- Status enum is the superset of both docs' lifecycles: candidate/active/probation/retired/archived/quarantined.
- Imperative voice ("you must...") = quarantine (human must look) but not hard reject; injection-shaped text (deny-list) = hard reject + quarantine.

## Next session
Phase 1 leftovers (`raph doctor`, evidence writer), then Phase 2: transcript locator + episode detectors + `raph mine`.
